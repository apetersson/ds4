---
id: DS-001.04
title: Implement selective resumable HF artifact acquisition
status: Done
assignee:
  - '@codex'
created_date: '2026-08-25 23:50'
updated_date: '2026-08-26 02:54'
labels:
  - hf
  - download
  - cache
dependencies:
  - DS-001.03
modified_files:
  - Makefile
  - ds4_hf.h
  - ds4_hf.c
  - tests/test_hf_cache.py
  - tests/test_hf_cache_probe.c
parent_task_id: DS-001
priority: high
type: feature
ordinal: 9000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Acquire only the selected receiver and runtime-requested companions. DS4 server vision uses tower/projector/config assets; llama.cpp consumes its separate mmproj GGUF. The resolver must never download both representations merely because both are cataloged.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Selecting Headroom128 never downloads Quality128, native reference, MLX, evaluations, or unrelated variants.
- [x] #2 ds4 text-only or --no-vision downloads only receiver files; ds4-server auto-vision downloads only receiver plus ds4_vision roles; --dspark adds only the matching DSpark role.
- [x] #3 The sibling llama_cpp_mmproj path is exposed by listing/dry-run metadata but is not downloaded by DS4 unless an explicit catalog-materialization operation requests it.
- [x] #4 Transport supports HF LFS/Xet through a safe available client, resumes interruption, never publishes partial files, and reports precise prerequisites.
- [x] #5 Concurrent resolution cannot corrupt the cache; a successful second launch reuses unchanged verified files.
- [x] #6 Cache location follows CLI/environment/default semantics, supports spaces, and records immutable repository revision and artifact role.
- [x] #7 Errors include repository, revision, selector, role, expected size, and destination without leaking credentials.
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Add a narrow acquisition-plan API that selects only the requested runtime roles while retaining unselected llama.cpp mmproj metadata for later listing/dry-run consumers.
2. Resolve CLI/environment/default cache roots into revision-pinned destinations and atomic identity sidecars containing repository, immutable commit, selector, role, size, and manifest digest.
3. Implement libcurl HTTP(S) redirect/range transport with resumable private partials, per-artifact interprocess locks, exact-size publication, safe credential handling, and contextual diagnostics.
4. Add deterministic fake-Hub tests for role selectivity, spaces/cache precedence, LFS/Xet-style redirects, interrupted resume, concurrency, reuse, metadata, and credential-safe errors.
5. Run focused HF gates plus proportional build validation; commit and iterate through fresh read-only reviews until exact ALL GOOD.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Implemented selected-variant role planning, revision-pinned cache destinations and identity sidecars, resumable libcurl LFS/Xet redirect transport, per-artifact process locking, and exact-size atomic publication. Deterministic HF cache suite passes 8 tests; ASan/UBSan focused run passes with leak detection disabled because Apple ASan does not support it. The broad make test gate builds and passes through evaluator/agent/math tests, then stops only because ds4flash.gguf is absent; every remaining deterministic command from that gate passes when run directly.

Fresh read-only iterative-review pass on efed2ff7 assessed the full DS-001.04 scope with DS-001.01-.03 integrated and returned exactly ALL GOOD; no findings or child tasks were required.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Implemented selective, revision-pinned HF acquisition for the chosen catalog variant with receiver/DS4 vision/DSpark role planning, metadata-only llama.cpp mmproj exposure, resumable cross-host LFS/Xet redirects, cache locks, exact-size atomic publication, identity sidecars, offline reuse, and credential-safe contextual errors. Verified by 8 deterministic cache tests, 6 immutable transport tests, the full HF manifest/CLI suites, ASan/UBSan, and all model-independent make test commands; the only unavailable gate is live ./ds4_test because ds4flash.gguf is absent. Final fresh parent-scoped reviewer verdict: ALL GOOD.
<!-- SECTION:FINAL_SUMMARY:END -->
