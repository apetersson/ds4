---
id: DS-001.02
title: Specify and parse a dual-runtime versioned catalog manifest
status: Done
assignee: []
created_date: '2026-08-25 23:50'
updated_date: '2026-08-26 02:16'
labels:
  - hf
  - manifest
  - security
dependencies:
  - DS-001.01
references:
  - >-
    /Users/andreas/.codex/attachments/600735e3-7f66-458b-9a99-1ca6f289de6b/pasted-text.txt
  - 'https://github.com/ggml-org/llama.cpp/blob/master/common/download.cpp'
modified_files:
  - Makefile
  - ds4_hf.h
  - ds4_hf.c
  - tests/test_hf_manifest.c
  - tests/fixtures/hf/variants-v2.json
parent_task_id: DS-001
priority: high
type: feature
ordinal: 7000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Define variants.json schema v2 as DS4's typed integrity and capability manifest. It must describe a shared ordinary DeepSeek4 receiver, DS4-native vision components, a llama.cpp mmproj companion, and optional DSpark, while acknowledging that llama.cpp selects solely from repository paths, filenames, and GGUF metadata.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Schema defines repository/default selector plus per-variant receiver, ds4_vision, llama_cpp_mmproj, and optional dspark records with paths, byte counts, SHA-256, precision, profile, required/optional capability, and minimum runtime revision.
- [x] #2 Shared vision metadata records image token/id, 1024 image size, 896-to-4096 projection, 256 rows per view, one separator, preprocessing contract, and token formula views*256+1 rather than the incomplete layouts enumeration.
- [x] #3 Schema and fixtures account for 257, 769, 1025, and 1281-row cases and reject an implementation that assumes only the previous 257/769/1281 set.
- [x] #4 Unknown optional fields are ignored while unknown required capabilities and unsupported major versions fail actionably.
- [x] #5 Duplicate selectors, unsafe paths, malformed hashes/sizes, conflicting defaults, incomplete companion bundles, and mismatched runtime capability declarations are rejected within explicit parser resource limits.
- [x] #6 Manifest data cannot inject argv, environment, executable paths, shell fragments, or remote code; the encoder executable/interpreter remains trusted local configuration.
- [x] #7 Production Headroom128 and Quality128 fixtures prove that main/mmproj/dspark filenames also satisfy llama.cpp sibling discovery independently of the manifest.
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Define bounded v2 manifest data structures and a non-executable JSON parser; validate typed artifacts, capabilities, runtime constraints, vision metadata, row formula, duplicate/default/path/hash/size/bundle invariants, and independent llama.cpp sibling naming. Add production-shaped Headroom128 and Quality128 fixtures with focused positive and negative tests, then run relevant non-model gates and fresh parent-scoped iterative reviews.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Verification before review pass 1: make cpu passed; make -B all passed; focused test-hf-manifest and existing test-hf-cli passed; established non-model evaluator, agent, layer-pack, placement, GPU-args unit/CLI, sampling, Q4_K, and MXFP4 gates passed; Clang ASan/UBSan manifest suite passed; strict -Werror compile and git diff --check passed. Production-model ./ds4_test was not run because ds4flash.gguf is absent.

Review pass 1 dispositions: DS-001.02.01 Include now (High, required); DS-001.02.02 Include now (Medium, required); DS-001.02.03 Include now (Medium, required). All three were implemented. Verification before review pass 2 repeated CPU/native builds and all established non-model gates; focused and ASan/UBSan manifest suites pass; ds4_test remains unavailable because ds4flash.gguf is absent.

Review pass 2 dispositions: DS-001.02.04 Include now (Medium, required); DS-001.02.05 Include now (High, required). Both implemented. Verification before review pass 3 repeated CPU/native builds, all established non-model gates, focused manifest/CLI suites, strict compile, and ASan/UBSan; ds4_test remains unavailable because ds4flash.gguf is absent.

Review pass 3 disposition: DS-001.02.06 Include now (Medium, required). Implemented all current llama.cpp primary basename exclusions. Verification before review pass 4 repeated CPU/native builds, all established non-model gates, focused manifest/CLI suites, strict compile, and ASan/UBSan; ds4_test remains unavailable because ds4flash.gguf is absent.

Review pass 4 disposition: Include now (DS-001.02.07). Practical likelihood Medium: production-sized receivers are realistic split candidates. Schema v2 has one typed receiver record, so accepting either first or later shards would make primary selection/integrity incomplete. Implemented complete split-name rejection with first/non-first negative tests; awaiting final fresh reviewer.

Verification before review pass 5 repeated CPU/native builds, all established non-model gates, focused manifest/CLI suites, strict compilation, and ASan/UBSan; ds4_test remains unavailable because ds4flash.gguf is absent.

Review pass 5 disposition: Include now (DS-001.02.08), Medium practical likelihood and required for AC #2/#3. Added an exact typed crop-count rule so global-only or global plus 2-4 crops are valid and 513 rows are rejected. Verification before review pass 6 repeated all focused, sanitizer, CPU/native, and established safe non-model gates; ds4_test remains unavailable because ds4flash.gguf is absent.

Review pass 6 disposition: Include now (DS-001.02.09), Medium practical likelihood and required for AC #2/#7. Mirrored the exact crop-count rule into independent llama.cpp mmproj GGUF metadata validation with positive production summaries and negative mismatch coverage. Verification before review pass 7 repeated all focused, sanitizer, CPU/native, and established safe non-model gates; ds4_test remains unavailable because ds4flash.gguf is absent.

Review pass 7 disposition: Include now (DS-001.02.10), Low practical likelihood but required by AC #4. Separated bounded grammar-only JSON number skipping from strict typed numeric conversion and added large-exponent/long-integer forward-compatibility coverage. Verification before review pass 8 repeated all focused, sanitizer, CPU/native, and established safe non-model gates; ds4_test remains unavailable because ds4flash.gguf is absent.

Fresh read-only review pass 8 result: ALL GOOD. All findings DS-001.02.01 through DS-001.02.10 were explicitly dispositioned Include now, implemented, tested, and verified.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Specified and implemented the bounded, non-executable schema-v2 DS4 catalog parser with production-shaped dual-runtime fixtures, exact vision preprocessing and row contracts, typed integrity/capability validation, independent llama.cpp path/GGUF metadata compatibility checks, forward-compatible optional-field handling, and injection defenses. All seven ACs pass focused/full safe gates, strict C99, ASan/UBSan, and fresh iterative review result ALL GOOD.
<!-- SECTION:FINAL_SUMMARY:END -->
