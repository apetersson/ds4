---
id: DS-006.05
title: Implement the Qwen2 query encoder and donor attention mask
status: To Do
assignee: []
created_date: '2026-08-26 00:21'
labels:
  - llama.cpp
  - libmtmd
  - ggml
  - qwen2
  - attention
dependencies:
  - DS-006.04
parent_task_id: DS-006
priority: high
type: feature
ordinal: 16000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Implement/reuse the 24-layer Qwen2-as-encoder stage that appends 256 learned query rows to the 256 SAM rows, applies the donor's mixed bidirectional/causal attention mask, and returns the final query rows.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 The graph appends exactly 256 learned query vectors after the 256 image rows with correct position and rotary semantics.
- [ ] #2 Image rows attend bidirectionally to image rows; query rows attend to all image rows and only preceding/own query rows exactly as the donor mask specifies.
- [ ] #3 All 24 layers, norms, attention projections, rotary handling, MLPs, and residuals map to required BF16 tensors with exact shapes.
- [ ] #4 Only final query rows are selected, producing 256x896 per view in canonical order.
- [ ] #5 Mask values and selected layer/final outputs match saved PyTorch golden tensors within declared tolerances for multiple view batches.
- [ ] #6 Tests fail specifically when the mask is replaced by fully causal, fully bidirectional, or off-by-one variants.
<!-- AC:END -->
